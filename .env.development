# Local development only — npm run dev uses this automatically.
# Production deploy (Render) uses dashboard env vars, NOT this file.
# Empty VITE_API_URL = Vite proxy forwards /api and /uploads to localhost:3000
VITE_API_URL=
VITE_STORE_URL=http://localhost:5173